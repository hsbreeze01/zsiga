# Proposal: implement adaptive turn limit in config.py and orchestrator.py: calculate max_turns for each pipeline phase based on project complexity score from project_context, simple projects get fewer turns while complex multi-file projects get more, configurable via zsiga.yaml pipeline.adaptive_turns section

## Summary
implement adaptive turn limit in config.py and orchestrator.py: calculate max_turns for each pipeline phase based on project complexity score from project_context, simple projects get fewer turns while complex multi-file projects get more, configurable via zsiga.yaml pipeline.adaptive_turns section

## Motivation

## Expected Behavior

