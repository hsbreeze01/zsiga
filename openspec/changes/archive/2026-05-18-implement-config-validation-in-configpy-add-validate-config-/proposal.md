# Proposal: implement config validation in config.py: add validate_config() function that checks all required fields in zsiga.yaml, reports missing/invalid values, and provides defaults for optional fields

## Summary
implement config validation in config.py: add validate_config() function that checks all required fields in zsiga.yaml, reports missing/invalid values, and provides defaults for optional fields

## Motivation

## Expected Behavior

