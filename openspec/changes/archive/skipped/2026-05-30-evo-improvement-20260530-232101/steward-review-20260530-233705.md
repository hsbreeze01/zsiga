## Verdict: PUSHBACK

## 我的判断

这个 proposal 的目标（给 daemon 加测试）是合理的，技术路径也没问题，但它的核心前提是**虚假的**。proposal 反复强调"无测试模块"、"缺少测试文件"，但 `tests/` 目录下已有 **5 个 daemon 测试文件、48 个测试函数**在覆盖 `zsiga/daemon.py`：`test_daemon_state.py`(10)、`test_daemon_scheduling.py`(9)、`test_daemon_cycle_resilience.py`(6)、`test_dashboard_api.py`(13)、`test_dashboard_queue.py`(10)。一个声称"没有测试"的 proposal 无法令人信任它的覆盖分析是准确的。更糟的是，proposal 的"函数列表"只列了 10 个函数，但它自己声明文件有 21 个函数——这意味着高 CC 函数 `_build_pipeline_status`(CC=32) 和 `daemon_loop`(CC=51) 的覆盖空白没有被正确分析。在补充覆盖缺口之前，需要先诚实面对已有覆盖。

## 评分详情
- 可行性: 2/2 -- 目标模块 `zsiga/daemon.py` 及列出的所有函数均经代码验证存在，无争议
- 可执行性: 1/2 -- 列出了目标函数和 mock 策略方向，但未分析已有覆盖导致可能重复测试；函数列表仅列 10/21 个，遗漏了 `_detect_proposal_phase`、`_build_current_json`、`_health_check`、`_build_proposal_stats_json`、`_build_budget_analysis_json`、`_build_langfuse_summary`、`_build_evolution_status`、`_build_proposal_detail` 等函数
- 能力匹配: 2/2 -- 已有 48 个 daemon 测试成功运行，证明 agent 完全具备为此模块编写测试的能力
- 历史风险: 1/2 -- 无直接"写测试"类失败记录，但 `daemon.cycle_error` 在 learnings 中反复出现（4 次），表明 daemon 代码本身不稳定，测试可能需要频繁维护
- 范围合理性: 1/2 -- 范围定义看似清晰（新建一个文件，不改源码），但核心前提"无测试模块"是**事实性错误**（已有 48 个测试），函数列表不完整（10/21），这种不准确的现状描述会让执行方向跑偏
- 验收可测性: 2/2 -- 4 条 BAC 均为结构性检查（文件存在、函数名存在、计数 ≥3、pytest 退出码 0），格式正确，可自动验证
- 总分: 9/12

## 疑虑
1. **核心前提虚假**：proposal 声称"无测试模块 `zsiga/daemon.py`"，但 `tests/test_daemon_state.py` 已覆盖 `_write_daemon_state`(10 个 test)，`tests/test_dashboard_api.py` 已覆盖 `_build_status_json`/`_build_metrics_json` 等的 API 端点(13 个 test)，`tests/test_dashboard_queue.py` 已覆盖 `_scan_proposal_queue` 的行为(10 个 test)。新 proposal 可能产生大量重复测试。
2. **函数列表不完整**：列出 10 个函数但声称 21 个，遗漏了 `_build_proposal_detail`(CC=20)、`_build_evolution_status`(CC=11)、`_health_check`、`_build_proposal_stats_json` 等未覆盖的关键函数。覆盖缺口分析不准确。
3. **高 CC 函数的测试可行性未评估**：`daemon_loop`(CC=51, 309 行) 和 `_build_pipeline_status`(CC=32) 的 mock 依赖极深（`sqlite3`、`Path` 文件系统、`load_config`、`load_all_changes`、`asyncio.run`），proposal 只笼统说"使用 mock 隔离"但没给出具体 mock 策略。

## 建议
1. **修正现状描述**：承认已有 5 个测试文件 48 个测试函数的存在，用 `pytest --cov=zsiga.daemon` 或 `coverage.py` 获取精确的行覆盖率数据，基于真实覆盖率缺口而非虚假的"零覆盖"前提来定义工作范围。
2. **补充完整的未覆盖函数清单**：对比已有测试覆盖的函数与 daemon.py 全部 21 个导出符号，列出真正缺少测试的函数（如 `_build_proposal_detail`、`_build_current_json`、`_health_check`、`_build_proposal_stats_json`、`_build_evolution_status`、`_detect_proposal_phase` 等），并标注每个函数的 mock 难度。
3. **更新 BAC**：在 BAC-02 中避免与已有测试重复的函数名，改为覆盖真正未被测试的函数（如 `test__health_check`、`test__build_proposal_detail`、`test__build_current_json`）。

## 历史参考
- FAIL: verify-layer0-with-tests at verify (2026-05-27) — 类似"添加测试"类 proposal 在 verify 阶段失败
- RECURRING: daemon.cycle_error (4 occurrences 2026-05-27~29) — daemon 代码本身不稳定，新测试需关注自身健壮性
