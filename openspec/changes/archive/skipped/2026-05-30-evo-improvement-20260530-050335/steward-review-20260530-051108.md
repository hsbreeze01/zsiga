## Verdict: REJECT

## 我的判断

这是一个令我沮丧的 proposal。`zsiga/harness/runner.py` 早已通过 `tests/test_harness_runner.py`（277 行，28 个测试函数）获得了全面的测试覆盖，但自演进引擎因为只匹配 `test_{module_basename}.py`（即 `test_runner.py`），永远发现不了 `test_harness_runner.py` 的存在，于是第 27+ 次生成了同一个废 proposal。这不是一个需要评估的需求——这是一个需要修复引擎 bug 的信号。我将直接拒绝。

## 评分详情
- **可行性: 0/2** — 核心前提为假。`zsiga/harness/runner.py` 已有 `tests/test_harness_runner.py` 覆盖（277行，28个`def test_`函数），proposal 声称"缺少测试文件"完全不符合事实。确定性事实验证 `tests/test_runner.py` 不存在是正确的，但这是错误的文件名——真正的测试文件早已存在。
- **可执行性: 1/2** — 有具体文件名和 BAC，但目标文件名 `test_runner.py` 是错的（应为 `test_harness_runner.py`），静态分析数据严重失真（声称"0 函数"实际有大量方法），BAC-02 引用的 `test_module_smoke` 符号根本不存在于项目中。
- **能力匹配: 0/2** — 同名 `add-tests-runner` proposal 已连续失败 26+ 次，全部被 skip/reject，成功率为 0%。
- **历史风险: 0/2** — 这是完全相同的空转循环，第 27+ 次发生。auto-generated proposal 自动 -1，本项直接封底。根本原因是引擎的测试发现逻辑只匹配 `test_{basename}.py` 而不匹配 `test_{parent}_{basename}.py` 模式。
- **范围合理性: 1/2** — 范围表面清晰，但执行结果会是创建一个与 `test_harness_runner.py` 重复的测试文件，制造混乱而非价值。
- **验收可测性: 1/2** — 有 BAC 结构但质量极差：BAC-02 要求的 `test_module_smoke` 在整个项目中不存在；BAC-01 要求创建的 `test_runner.py` 文件名不符合项目命名惯例。
- **总分: 3/12**

## 疑虑
1. **测试已存在**：`tests/test_harness_runner.py`（277行，28个测试）已全面覆盖 `zsiga/harness/runner.py`，包括 `TestEventDataclasses`、`TestHarnessResult`、`TestHarnessRunnerDiscover`、`TestHarnessRunnerRun`、`TestHarnessRunnerPytestFailClosed` 等。proposal 的核心问题陈述"模块缺少测试文件"为假命题。
2. **27+ 次空转循环**：archive 中可追溯 26+ 个同名 proposal（`openspec/changes/archive/skipped/` 和 `openspec/changes/archive/2026-05-29-bulk-stall/`），全部被 skip/reject，是 pipeline 历史上最严重的空转案例。
3. **静态分析数据失真**：proposal 声称"函数数: 0"，但 `HarnessRunner` 有 `discover`、`run`、`results`、`_run_file`、`run_pytest` 等方法，`_HarnessCollectorPlugin` 有 5 个 pytest hook 方法。输入数据的不可靠性导致整个 proposal 的分析基础崩塌。
4. **BAC 引用不存在的符号**：`test_module_smoke` 在整个项目中找不到定义（确定性事实已确认），BAC-02 是一个无法通过的检查。

## 建议
1. **修复引擎 bug 而非执行 proposal**：在 `zsiga/intake/evolution.py`（或相关静态分析模块）中修改测试文件发现逻辑，使其不仅匹配 `test_{basename}.py`，还能匹配 `test_{parent}_{basename}.py` 和 `test_{grandparent}_{parent}_{basename}.py` 等模式。这才是 27 次空转的根因。
2. **添加去重检查**：在 proposal 生成阶段增加"已有覆盖检测"步骤，如果目标模块已有测试文件（不管命名方式），应跳过 proposal 生成。
3. **将 `add-tests-runner` 加入永久黑名单**：在 evolution 配置中添加规则，禁止为 `harness/runner.py` 再生成 `add-tests-*` proposal。

## 历史参考
- FAIL: `add-tests-runner` × 26+ 次 at steward/skip (2026-05-27~2026-05-30) — 全部因已有测试覆盖被 skip/reject，pipeline 历史上最严重的空转循环
- FAIL: verify-layer0-with-tests at verify (2026-05-27) — review error and adjust approach
