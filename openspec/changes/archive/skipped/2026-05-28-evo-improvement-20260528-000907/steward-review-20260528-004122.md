## Verdict: PUSHBACK

## 我的判断

这个 proposal 的动机是对的——`zsiga/daemon.py` 的 `_scan_proposal_queue`、`_build_pipeline_status`、`_build_proposal_detail` 等高复杂度函数确实缺少直接测试覆盖。**但它的 problem statement 存在严重的事实错误**：它声称"模块 `zsiga/daemon.py` 缺少测试文件"，实际上 `tests/` 目录下已有 **3 个 daemon 相关测试文件共 876 行**：`test_daemon_state.py`(242L, 10个测试)、`test_daemon_scheduling.py`(421L, 12个测试)、`test_daemon_cycle_resilience.py`(213L, 7个测试)。这些文件已经覆盖了 `_write_daemon_state`、`_read_daemon_state`、`daemon_loop`、`_daemon_state_path` 等函数。

更让我不满的是 **BAC 与声称的优先级自相矛盾**：proposal 说"优先覆盖高复杂度函数: `_scan_proposal_queue`(CC=29), `_build_pipeline_status`(CC=32)"，但 BAC-02 只要求 `test__lock_path`、`test__daemon_state_path`、`test__read_daemon_state`——这些是 4~9 行的极简函数，而后两者已被现有测试间接覆盖。执行者可以通过所有 BAC 而完全不触碰那 6 个 CC>10 的高风险函数。

## 评分详情
- 可行性: 2/2 -- 目标文件 `zsiga/daemon.py` 存在（已验证1056行），所有提到的函数（`_lock_path`、`_scan_proposal_queue`、`_build_pipeline_status` 等）均已在代码中确认。创建 `tests/test_daemon.py` 无技术障碍。
- 可执行性: 1/2 -- 有方向（mock隔离、优先高CC函数），但proposal未意识到已有876行daemon测试存在，且BAC只覆盖最简单的函数。`_scan_proposal_queue` 内部依赖 `config.load_config()`、`metrics.db.load_all_changes()` 等模块，proposal未给出具体的mock策略。
- 能力匹配: 1/2 -- 项目中已有70+测试文件，测试编写模式成熟（monkeypatch、tmp_path、mock patch均有先例），但无"add-tests-daemon"类proposal的成功记录。
- 历史风险: 1/2 -- 此proposal由自演进引擎生成（约束中明确说明），适用auto-generated -1规则。同时有`daemon.cycle_error`重复失败记录（2026-05-27三次），`verify-layer0-with-tests`在verify阶段失败。
- 范围合理性: 2/2 -- 只添加测试文件，不修改daemon.py源码，范围独立、无副作用。BAC-04要求pytest通过确保不会引入破坏性代码。
- 验收可测性: 2/2 -- 4条BAC均为binary可自动验证：文件存在、函数名存在、计数≥3、pytest退出码0。格式规范。
- 总分: 9/12

## 疑虑
1. **Problem statement 事实错误** — proposal 声称 `daemon.py` 缺少测试，但 `test_daemon_state.py`(242L)、`test_daemon_scheduling.py`(421L)、`test_daemon_cycle_resilience.py`(213L) 已存在并覆盖了 `_write_daemon_state`(10个测试)、`daemon_loop`(12个测试)、错误恢复(7个测试)。Proposal 基于错误的分析数据做出判断。

2. **BAC 与优先级脱节** — 声称"优先覆盖高CC函数"（CC=29的`_scan_proposal_queue`、CC=32的`_build_pipeline_status`），但 BAC-02 只检查 `test__lock_path`（6行函数）、`test__daemon_state_path`（4行函数）、`test__read_daemon_state`（9行函数）。执行者可以100%通过BAC却完全忽略高价值测试目标。

3. **高复杂度函数的mock难度被低估** — `_scan_proposal_queue`（CC=29，90行）内部有嵌套try/except、lazy import（`from .config import load_config`、`from .metrics.db import load_all_changes`），以及多层文件系统遍历。`_build_pipeline_status`（CC=32）依赖SQLite查询和多层JSON解析。Proposal仅说"使用mock隔离"而未设计具体的mock边界。

## 建议
1. **重写 Problem Statement** — 应明确列出哪些函数已有测试覆盖（`_write_daemon_state`、`_read_daemon_state`、`daemon_loop`），哪些尚未覆盖（`_scan_proposal_queue`、`_build_pipeline_status`、`_build_proposal_detail`、`_build_proposal_stats_json`、`_health_check`、`_compute_uptime_seconds`、`acquire_lock`/`release_lock`等），避免重复劳动。

2. **BAC 与优先级对齐** — 至少添加一条BAC要求覆盖高CC函数，例如：
   - `[BAC-05] tests/test_daemon.py 中存在 test__scan_proposal_queue`
   - `[BAC-06] tests/test_daemon.py 中存在 test__compute_uptime_seconds`
   
3. **补充 mock 策略** — 对 `_scan_proposal_queue` 应明确：用 `tmp_path` 构造虚拟 changes 目录，monkeypatch `ZSIGA_HOME`，mock `load_config` 和 `load_all_changes` 返回值。对 `_health_check` 和 `_build_proposal_stats_json` 应说明用 `tmp_path` 构造 SQLite 数据库。

## 历史参考
- FAIL: verify-layer0-with-tests at verify (2026-05-27) — 测试类 proposal 在验证阶段失败的先例
- daemon cycle #1 failed (2026-05-26) — [permanent] OperationalError: duplicate column name: steward_verdict — daemon模块的SQLite操作有历史风险
