## Verdict: PUSHBACK

## 我的判断

这个 proposal 有一个根本性的问题：它在 Problem 部分声称模块"缺少测试文件"，但实际上 `tests/test_phase_duration.py`（241 行，6 个测试类，15 个测试用例）**已经从 `zsiga/duration_predictor` 导入并测试了该模块的核心函数**。proposal 的前提就是不成立的——它不是在为"无测试模块"添加首个测试，而是在为一个已有充分测试覆盖的模块创建第二个测试文件。如果目的是为 `_collect_known_phases` 和 `_predict_phase` 添加直接单元测试（目前仅通过 `predict_change_duration` 间接覆盖），完全可以在现有文件中补充，而不需要新建文件。我不反对加强测试覆盖，但我反对基于错误前提创建重复的测试文件。

## 评分详情
- 可行性: 2/2 -- 所有 5 个函数确认存在于 `zsiga/duration_predictor.py`，模块纯 Python 无外部依赖，技术上完全可行
- 可执行性: 2/2 -- 目标文件、函数名、BAC 都非常具体，有明确的变更路径
- 能力匹配: 1/2 -- 近期有 `verify-layer0-with-tests` 的失败记录，但模式不完全相同；无同类任务的明确成功记录
- 历史风险: 0/2 -- auto-generated proposal（Constraints 明确标注"由 zsiga 自演进引擎生成"）默认 -1；且有 `verify-layer0-with-tests` at verify 的失败记录，基分为 1 → 最终 0
- 范围合理性: 1/2 -- 范围本身清晰，但 proposal 核心前提（"无测试模块"）与事实不符。创建 `tests/test_duration_predictor.py` 会与已有的 `tests/test_phase_duration.py` 产生功能重叠，scope 描述不够诚实
- 验收可测性: 2/2 -- 4 条 BAC，全部可自动验证：文件存在、函数名存在、test 数量、pytest 退出码
- **总分: 8/12**

## 疑虑
1. **核心前提虚假** — proposal 声称"模块 `zsiga/duration_predictor.py` 缺少测试文件"，但 `tests/test_phase_duration.py` 已包含 15 个测试用例覆盖该模块。`_fit_linear` 有直接测试（`TestFitLinear`），`predict_change_duration` 有 6 个测试类覆盖充足/不足数据两条路径，`_collect_known_phases`、`_predict_phase`、`_fallback_estimates` 通过集成测试间接覆盖。
2. **重复投资风险** — 新建 `tests/test_duration_predictor.py` 与现有 `tests/test_phase_duration.py` 功能重叠，增加维护负担而无实质新增覆盖。
3. **auto-generated 循环风险** — 自演进引擎生成此类"补测试" proposal 容易产生循环：发现缺测试 → 建议加测试 → 忽略已有测试 → 重复劳动。

## 建议
1. **修改 proposal 前提** — 承认 `tests/test_phase_duration.py` 已存在，明确说明当前覆盖缺口（仅 `_collect_known_phases` 和 `_predict_phase` 缺少直接单元测试）。
2. **改为在现有文件中补充测试** — 在 `tests/test_phase_duration.py` 中添加 `TestCollectKnownPhases` 和 `TestPredictPhase` 两个测试类，而非新建文件。相应修改 BAC-01 和 BAC-02。
3. **如果坚持新建文件** — 需要在 proposal 中说明为什么不扩展已有文件、两个测试文件之间的职责划分，以及如何避免重复。

## 历史参考
- FAIL: verify-layer0-with-tests at verify (2026-05-27) — 测试验证阶段的失败模式，提示测试类 proposal 需要更谨慎地验证现有覆盖状态
