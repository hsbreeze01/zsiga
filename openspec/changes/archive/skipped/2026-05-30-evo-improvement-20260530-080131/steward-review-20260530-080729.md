## Verdict: PUSHBACK

## 我的判断

我拒绝这个 proposal 的核心原因：**它的前提是错的**。Proposal 声称"模块 `zsiga/duration_predictor.py` 缺少测试文件"——但 `tests/test_phase_duration.py`（241 行）已经对 `duration_predictor.py` 做了相当完整的测试。该文件直接导入了 `_fit_linear` 和 `predict_change_duration`，包含 9 个测试方法覆盖了回归拟合、回退逻辑、负值钳位、缺失阶段键等场景。这是典型的 auto-generated proposal 的盲目性——静态分析只检查了 `tests/test_duration_predictor.py` 这个命名模式是否存在，却没有检查模块是否已有测试覆盖。为一个已有测试的模块创建第二个测试文件，是低价值的重复劳动。

## 评分详情
- 可行性: 2/2 -- 源模块和所有函数均已确认存在，创建测试文件技术上无障碍。
- 可执行性: 2/2 -- BAC 结构清晰，目标文件、函数名、验收条件都很具体。
- 能力匹配: 1/2 -- 无近期同类测试编写任务的成功/失败记录可参考。
- 历史风险: 0/2 -- 有相关失败 `verify-layer0-with-tests`；且本 proposal 为 auto-generated（-1 惩罚）。auto-generated proposal 容易基于不完整前提生成，正如此例。
- 范围合理性: 1/2 -- 范围看似清晰，但**问题陈述具有误导性**：声称模块"缺少测试"，实际上 `tests/test_phase_duration.py` 已有 9 个测试方法覆盖该模块的主要函数。创建同名测试文件本质上是重复劳动。
- 验收可测性: 2/2 -- BAC-01 到 BAC-04 均为 binary check，可自动验证。
- **总分: 8/12**

## 疑虑

1. **已有测试覆盖，前提不成立**：`tests/test_phase_duration.py` 已直接 `from zsiga.duration_predictor import _fit_linear, predict_change_duration`，包含 `TestFitLinear`（2 tests）、`TestPredictChangeDurationSufficient`（2 tests）、`TestPredictChangeDurationInsufficient`（3 tests）、`TestNegativeClamping`（1 test）、`TestMissingPhaseKeys`（1 test），共 9 个测试方法。Proposal 说"缺少测试文件"是假阴性。

2. **重复文件无实质增量**：BAC-02 要求 `test__collect_known_phases`、`test__fit_linear`、`test__predict_phase`，但 `_fit_linear` 和 `predict_change_duration`（内部调用 `_predict_phase`）已有直接测试。仅有 `_collect_known_phases` 和 `_fallback_estimates` 目前是间接覆盖，但这不构成创建整个新测试文件的理由。

3. **auto-generated proposal 的模式盲区**：静态分析只按文件名 `test_<module>.py` 匹配，未检测跨文件测试覆盖，导致误判。

## 建议

1. **改为在现有文件中补充测试**：如果确实想增强覆盖，应在 `tests/test_phase_duration.py` 中新增 `TestCollectKnownPhases` 和 `TestFallbackEstimates` 两个测试类，而非创建新文件。修改 proposal 的 Target Files 和 BAC 条件。

2. **如果坚持创建独立文件**：需要在 Problem 部分如实说明"已有 `tests/test_phase_duration.py` 覆盖部分函数，本 proposal 目标是补充 `_collect_known_phases`、`_predict_phase`、`_fallback_estimates` 的直接测试，并统一归入按模块命名的测试文件"——这才是诚实的问题陈述。

3. **静态分析工具应改进**：检测"无测试模块"时，不应只检查 `test_<module>.py`，应 grep 所有 test 文件中对该模块的 import。

## 历史参考
- FAIL: verify-layer0-with-tests at verify (2026-05-27) — review error and adjust approach
