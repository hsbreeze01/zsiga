## Verdict: REJECT

## 我的判断

这个 proposal 是一个典型的"幽灵循环"——它基于一个**不存在的前提**：`zsiga/harness/runner.py` 缺少测试。事实是 `tests/test_harness_runner.py`（277 行，16 个测试函数，覆盖全部 10 个类）已经全面覆盖了该模块。Proposal 引擎的 `_scan_code_structure()` 用 `basename` 匹配文件名，只能找到 `test_runner.py`，完全忽略了实际存在的 `test_harness_runner.py`。这个 proposal 已被生成了 **27+ 次，全部 skip/reject**，每一次都是空转浪费。创建一个 `test_runner.py` 要么是重复已有测试（无价值），要么创建空壳测试（有害）。**真正需要修复的是 proposal 引擎的测试文件发现逻辑，而不是给 runner.py 再写一份测试。**

## 评分详情

- **可行性: 1/2** — `runner.py` 确实存在（确定性事实确认），但 proposal 的核心前提"缺少测试"是**错误的**。`tests/test_harness_runner.py` 已包含 `TestEventDataclasses`、`TestHarnessResult`、`TestHarnessRunnerDiscover`、`TestHarnessRunnerRun`、`TestHarnessRunnerPytestFailClosed` 五个测试类共 16 个测试函数，覆盖全部公开类。目标模块存在，但"问题"不存在。
- **可执行性: 1/2** — 有明确的 target files 和 BAC，但 Technical Design 质量很低：声称"为公开函数编写测试"但静态分析本身承认有 0 个函数（全是类）；声称"优先覆盖高 CC 函数"但又说无高 CC 函数。执行路径是空泛的。
- **能力匹配: 0/2** — Scout 报告此 proposal 已被生成 27+ 次，全部 skip/reject。这是**连续失败**的典型模式，无任何成功记录。
- **历史风险: 0/2** — 完全相同的失败模式刚刚发生过（27+ 次）。这不是"相似"的失败，而是**完全相同的 proposal**被引擎反复吐出、反复被拒。
- **范围合理性: 1/2** — Scope 声明清晰（只添加测试、不修改源码），但基于虚假前提。创建 `test_runner.py` 要么是 `test_harness_runner.py` 的冗余副本，要么是无意义的空壳。真正的修复点不在 scope 内（proposal 引擎的 basename 匹配逻辑）。
- **验收可测性: 2/2** — 4 条 BAC 格式规范（文件存在、符号存在、函数计数、pytest 退出码），结构化且可自动验证。这是本 proposal 唯一做得好的地方。
- **总分: 5/12**

## 疑虑

1. **核心前提虚假**：确定性事实显示 `tests/test_harness_runner.py` 已存在，覆盖了 `TestEvent`、`TestStarted`、`TestPassed`、`TestFailed`、`TestError`、`HarnessResult`、`TestReport`、`QualificationReport`、`HarnessRunner`、`_HarnessCollectorPlugin` 全部 10 个类。代码验证已确认该文件包含 16 个 `def test_` 函数。Proposal 引擎因 basename 匹配 bug 未能发现它。
2. **27+ 次循环空转**：从 `__pycache__` 中可见大量 `test_spec_evo_improvement_*_runner_*` 文件，说明引擎反复尝试为 runner 添加测试，每次都因重复被拒。这是系统性的 proposal 生成缺陷。
3. **BAC-04 存在潜在误导**：如果创建了只含 `test_module_import` 和 `test_module_smoke` 的空壳文件，pytest 确实能退出码 0，但这完全不能证明"测试覆盖"的质量。

## 建议

1. **将 `add-tests-runner` 加入 proposal 黑名单**——该模块已有充分测试覆盖，无需重复。
2. **修复 proposal 引擎的测试文件发现逻辑**：`_scan_code_structure()` 中 `os.path.basename(pf).replace(".py", "")` 应改为基于模块路径的匹配（如 `harness/runner` → `test_harness_runner`），而非简单的 basename。这才是根本原因。
3. **在 proposal 生成前增加"已有测试检查"步骤**：扫描 `tests/` 目录下所有匹配 `*runner*` 的文件（glob 或 fuzzy match），而非精确匹配 `test_{module}.py`。

## 历史参考

- FAIL: `add-tests-runner` × 27+ — 全部在 proposal/skip 阶段被拒，原因均为"测试已存在于 `test_harness_runner.py`"（从 `__pycache__` 中的 `test_spec_evo_improvement_*_runner_*` 文件可追溯）
- FAIL: `verify-layer0-with-tests` at verify (2026-05-27) — review error and adjust approach
