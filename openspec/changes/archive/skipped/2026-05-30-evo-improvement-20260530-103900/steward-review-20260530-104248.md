## Verdict: PUSHBACK

## 我的判断

这个 proposal 的核心问题——`_collect_known_phases`、`_predict_phase`、`_fallback_estimates` 三个函数缺少直接单元测试——是真实存在的。但 proposal 的分析存在严重盲区：它声称模块"缺少测试文件"，却完全忽略了 `tests/test_phase_duration.py`（241 行，9 个测试）已经覆盖了 `_fit_linear` 和 `predict_change_duration`。如果按 proposal 原样执行，我们会得到**两个测试文件覆盖同一个 164 行模块**，这是明显的冗余和代码卫生问题。我拒绝在这个前提下直接放行。

## 评分详情
- **可行性: 2/2** — 目标模块 `zsiga/duration_predictor.py` 确认存在，5 个函数全部可定位。需要新建的文件不存在（BAC-01 合理）。
- **可执行性: 1/2** — 有明确的函数列表和 BAC，但完全忽略了已有测试文件 `tests/test_phase_duration.py`。没有说明是应该创建独立文件还是在已有文件上扩展，执行者会面临歧义。
- **能力匹配: 1/2** — 无同类任务的成功记录。历史中仅有一个模糊相关的失败 (`verify-layer0-with-tests`)。
- **历史风险: 1/2** — auto-generated proposal（默认 -1）。无直接失败模式，但自动生成的 proposal 容易产生分析盲区，本 proposal 正是如此。
- **范围合理性: 1/2** — 范围本身不大（164 行模块），但 proposal 的"问题陈述"与实际不符：声称模块缺少测试，实际上已有 9 个测试。创建第二个测试文件是浪费。修改 pipeline 自身代码的上限限制不适用，但"忽略已有代码"同样应该扣分。
- **验收可测性: 2/2** — BAC 结构清晰：文件存在、函数名存在、≥3 个 test_ 函数、pytest 退出码 0。4 条 Binary Acceptance Checks，覆盖完整。
- **总分: 8/12**（PUSHBACK 区间）

## 疑虑

1. **已有测试文件被完全忽略** — `tests/test_phase_duration.py`（241 行）已经包含 `TestFitLinear`（2 测试）、`TestPredictChangeDurationSufficient`（2 测试）、`TestPredictChangeDurationInsufficient`（3 测试）、`TestNegativeClamping`（1 测试）、`TestMissingPhaseKeys`（1 测试）。Proposal 声称"模块缺少测试文件"，这是不准确的。如果创建 `tests/test_duration_predictor.py`，将为同一个 164 行模块维护两个测试文件，违反 DRY 原则。

2. **真实覆盖缺口比描述的小** — 实际缺失的直接测试仅限于 `_collect_known_phases`、`_predict_phase`、`_fallback_estimates` 三个私有函数，它们已通过 `predict_change_duration` 的多条路径间接覆盖。proposal 没有区分"完全无测试"和"有间接覆盖但缺直接测试"。

## 建议

1. **重新定位目标**：不要创建新的 `tests/test_duration_predictor.py`，而是在现有的 `tests/test_phase_duration.py` 中补充对 `_collect_known_phases`、`_predict_phase`、`_fallback_estimates` 的直接测试。或者，如果坚持统一命名，应先将 `test_phase_duration.py` 重命名为 `test_duration_predictor.py`，再追加缺失的测试。

2. **更新 BAC**：BAC-01 和 BAC-02 需要相应调整——要么目标是增强已有文件，要么明确说明包含重命名步骤。当前的 BAC 暗示"从零创建"，与事实不符。

3. **补充覆盖缺口分析**：proposal 应明确列出已有覆盖和缺失覆盖的对比，而不是笼统地说"缺少测试文件"。

## 历史参考
- FAIL: verify-layer0-with-tests at verify (2026-05-27) — 同类"为模块添加测试"任务，教训是 "review error and adjust approach"，暗示执行前的分析质量直接影响成功率。本 proposal 的分析盲区正是同一模式。
