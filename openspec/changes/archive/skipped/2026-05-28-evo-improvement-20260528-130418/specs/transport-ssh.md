# transport-ssh — CONSOLIDATED

> **Superseded by `transport-unit-tests.md`** — all scenarios merged into the
> canonical spec under "Requirement: SSHTransport initialization",
> "Requirement: SSHTransport target formatting", "Requirement: SSHTransport
> base SSH arguments", "Requirement: SSHTransport control master setup",
> "Requirement: SSHTransport shell execution", and "Requirement: SSHTransport
> close".
