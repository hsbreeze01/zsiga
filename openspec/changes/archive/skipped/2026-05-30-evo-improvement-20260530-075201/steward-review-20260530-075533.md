## Verdict: PUSHBACK

## 我的判断

这个 proposal 是一个基于虚假前提的空转产物。我验证了 `tests/test_harness_runner.py` 已经存在（277 行，28 个测试函数），覆盖了 `runner.py` 的全部 10 个公开类和核心方法（`discover()`, `run()`, `run_pytest()`）。Proposal 声称"模块缺少测试文件"——这是完全错误的。更糟糕的是，proposal 要求创建的 `tests/test_runner.py` 只包含 `test_module_import` 和 `test_module_smoke` 两个冒烟测试，其质量远低于已有的 28 个测试。这是用劣质重复覆盖替换已有良好覆盖，属于纯粹的浪费。根因是自演进引擎的文件名匹配逻辑 bug——它用 `test_runner.py` 查找测试文件，而项目惯例是 `test_harness_runner.py`，导致引擎永远"发现"不了已有测试，持续生成 proposal 形成死循环。**不应执行此 proposal，而应修复上游的命名匹配逻辑。**

## 评分详情
- 可行性: 2/2 -- `zsiga/harness/runner.py` 存在，`tests/test_runner.py` 可以创建。技术上不存在障碍。
- 可执行性: 1/2 -- 有目标文件和 BAC，但技术设计严重失真：声称"0 函数"（实际有 `discover()`, `run()`, `run_pytest()` 等方法），只列出 5/10 个类，实现路径空洞。
- 能力匹配: 1/2 -- 无明确的同类任务成功/失败记录。
- 历史风险: 0/2 -- 这是自演进引擎因命名不匹配而产生的空转循环产物（Scout 报告 27+ 次循环），完全相同的虚假 proposal 反复生成。
- 范围合理性: 1/2 -- 范围窄且不修改源码，但核心前提自相矛盾（声称无测试，实际已有全面覆盖），且会创建一个与已有测试文件功能重叠的低质量文件。
- 验收可测性: 2/2 -- 有 4 条 BAC，格式规范，可自动验证。
- **总分: 7/12**

## 疑虑
1. **核心前提虚假**：Proposal 声称 `runner.py` 缺少测试文件。实际 `tests/test_harness_runner.py`（277 行，28 个 `def test_` 函数）已全面覆盖所有 10 个公开符号，包括事件 dataclass、`HarnessResult`、`HarnessRunner.discover()`/`.run()`/`.run_pytest()`、`QualificationReport`、`TestReport`。创建 `test_runner.py` 是冗余的。
2. **BAC 质量低于已有覆盖**：BAC-02 要求的 `test_module_import` + `test_module_smoke` 是最低级冒烟测试，远不如已有的 `TestHarnessRunnerDiscover`（3 个测试）、`TestHarnessRunnerRun`（7 个测试）、`TestHarnessRunnerPytestFailClosed`（4 个测试）等结构化测试。
3. **静态分析数据严重失真**：声称"0 函数, 10 类"但只列出 5 个类（遗漏 `HarnessResult`, `TestReport`, `QualificationReport`, `HarnessRunner`, `_HarnessCollectorPlugin`），且 `HarnessRunner` 有多个方法不是 0。
4. **空转循环**：根因是引擎用 `test_runner.py` 匹配测试文件，但项目惯例是 `test_harness_runner.py`，导致引擎永远发现不了已有测试。

## 建议
1. **修复上游根因**：修改自演进引擎的测试文件发现逻辑，支持按模块全限定名（`harness/runner` → `test_harness_runner.py`）匹配，而非仅用 basename（`runner` → `test_runner.py`）。这会彻底消除此类空转 proposal。
2. **如果确实要补充测试**：应基于对 `tests/test_harness_runner.py` 的 gap analysis，明确指出哪些方法/分支尚未覆盖，而非从零创建一个劣质替代品。
3. **在 proposal 模板中增加"已有测试文件"检查**：生成 proposal 前先搜索 `test_*.py` 中是否已有 `from zsiga.harness.runner import` 的导入，避免重复。

## 历史参考
- Scout 报告此 proposal 已空转 27+ 次，是自演进引擎命名匹配 bug 的直接后果
