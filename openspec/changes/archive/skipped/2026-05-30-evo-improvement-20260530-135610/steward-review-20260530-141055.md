## Verdict: PUSHBACK

## 我的判断

这个 proposal 的执行前提有一个**关键事实错误**：它声称 `zsiga/duration_predictor.py` "缺少测试文件"，但实际上 `tests/test_phase_duration.py` 已经包含了 9 个针对该模块的直接测试（`TestFitLinear`、`TestPredictChangeDurationSufficient`、`TestPredictChangeDurationInsufficient`、`TestNegativeClamping`、`TestMissingPhaseKeys`），覆盖了 `_fit_linear` 和 `predict_change_duration` 的核心路径。

Proposal 是可执行的，技术设计合理，BAC 结构优秀——但我不能无视一个在 problem statement 层面就出错的事实。如果一个 proposal 连"现状是什么"都没搞清楚就提解决方案，执行者很可能产出冗余测试（比如为已有充分覆盖的 `_fit_linear` 再写一遍），浪费 pipeline 资源。

Proposal 真正的价值在于 `_collect_known_phases`、`_predict_phase`、`_fallback_estimates` 这三个函数确实没有直接测试。但 proposal 完全没提到已有覆盖，也没区分哪些是增量、哪些是重复。

## 评分详情
- 可行性: 2/2 -- 目标模块 `zsiga/duration_predictor.py` 存在，5 个函数全部确认存在，新建测试文件路径清晰
- 可执行性: 1/2 -- 有函数名、行号、技术设计，但 **未识别已有测试覆盖**（`tests/test_phase_duration.py` 已有 9 个相关测试），BAC-02 要求的 `test__fit_linear` 已有充分覆盖（`TestFitLinear` 2 个 case），存在执行冗余风险
- 能力匹配: 1/2 -- 无近期同类"为已有部分测试的模块补全测试"的成功记录
- 历史风险: 2/2 -- 唯一历史教训 `FAIL: verify-layer0-with-tests` 模式为 `code.unknown`，与本案无直接关联
- 范围合理性: 1/2 -- 范围本身不大（单文件测试），但 problem statement 事实错误（声称"缺少测试"实际已有 9 个），导致 scope 定义不精确——执行者无法判断哪些测试是增量、哪些是重复
- 验收可测性: 2/2 -- BAC 结构优秀：文件存在检查（BAC-01）、具名函数检查（BAC-02）、数量阈值（BAC-03）、pytest 退出码（BAC-04），全部可自动验证
- 总分: 9/12

## 疑虑
1. **Problem statement 事实错误**：Proposal 声称 `zsiga/duration_predictor.py` "缺少测试文件"，但 `tests/test_phase_duration.py` 已包含 9 个直接测试该模块的 case（`TestFitLinear` 2个、`TestPredictChangeDurationSufficient` 2个、`TestPredictChangeDurationInsufficient` 3个、`TestNegativeClamping` 1个、`TestMissingPhaseKeys` 1个）。这意味着执行者可能重复造轮子。

2. **BAC-02 要求冗余测试**：要求 `tests/test_duration_predictor.py` 中存在 `test__fit_linear`，但 `_fit_linear` 已在 `test_phase_duration.py` 的 `TestFitLinear` 中有 2 个 case（已知系数恢复 + 空输入），且 `predict_change_duration` 的集成测试也间接覆盖了它。

3. **真正未覆盖的函数未在 BAC 中体现**：`_collect_known_phases`、`_predict_phase`、`_fallback_estimates` 确实没有直接测试，BAC-02 列了 `test__collect_known_phases` 和 `test__predict_phase` 但遗漏了 `_fallback_estimates`。

## 建议
1. **修正 problem statement**：明确承认 `tests/test_phase_duration.py` 已有的 9 个测试覆盖，列出已覆盖 vs 未覆盖的函数清单。未覆盖：`_collect_known_phases`、`_predict_phase`、`_fallback_estimates`。

2. **调整 scope 为纯增量**：Proposal 应聚焦于为 3 个无直接测试的函数（`_collect_known_phases`、`_predict_phase`、`_fallback_estimates`）添加测试，而非重新创建一个已部分覆盖的测试文件。或者，如果要创建独立文件 `tests/test_duration_predictor.py`，应明确说明与 `test_phase_duration.py` 的关系（替代？并存？）。

3. **修正 BAC-02**：移除 `test__fit_linear`（已有充分覆盖），添加 `test__fallback_estimates`（真正缺失的覆盖）。
