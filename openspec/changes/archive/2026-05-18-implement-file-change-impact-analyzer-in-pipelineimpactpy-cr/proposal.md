# Proposal: implement file change impact analyzer in pipeline/impact.py: create analyze_impact function that given a list of changed files returns downstream dependencies using import graph, estimated test scope based on which test files cover the changed modules, and risk level classification low medium high

## Summary
implement file change impact analyzer in pipeline/impact.py: create analyze_impact function that given a list of changed files returns downstream dependencies using import graph, estimated test scope based on which test files cover the changed modules, and risk level classification low medium high

## Motivation

## Expected Behavior

