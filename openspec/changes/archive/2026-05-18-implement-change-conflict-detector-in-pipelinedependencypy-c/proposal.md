# Proposal: implement change conflict detector in pipeline/dependency.py: create ChangeConflictDetector class with method scan_changes that reads all pending openspec changes, method find_overlaps that returns pairs of changes targeting the same files, method suggest_order that returns execution priority based on dependency graph

## Summary
implement change conflict detector in pipeline/dependency.py: create ChangeConflictDetector class with method scan_changes that reads all pending openspec changes, method find_overlaps that returns pairs of changes targeting the same files, method suggest_order that returns execution priority based on dependency graph

## Motivation

## Expected Behavior

