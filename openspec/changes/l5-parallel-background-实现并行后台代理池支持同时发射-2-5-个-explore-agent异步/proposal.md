# Proposal: l5-parallel-background: 实现并行后台代理池，支持同时发射 2-5 个 explore agent，异步收集结果后综合决策。核心接口 dispatch_many(tasks) → collect_all()，集成到 orchestrator 的 ENRICH 阶段

## Summary
l5-parallel-background: 实现并行后台代理池，支持同时发射 2-5 个 explore agent，异步收集结果后综合决策。核心接口 dispatch_many(tasks) → collect_all()，集成到 orchestrator 的 ENRICH 阶段

## Motivation

## Expected Behavior

