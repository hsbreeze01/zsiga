All spec requirements are verified against the diff. Let me write the review.

<tool_callTool>
{"name": "write_file", "arguments": {"path": "/home/zsiga/repo/openspec/changes/add-uptime-to-status-api/review.md", "content": "Verdict: CLEAN\n\nAll spec requirements are fully covered in the diff:\n\n1. **`uptime_seconds` field in daemon object**: The diff adds `\"uptime_seconds\": _compute_uptime_seconds(ds.get(\"started_at\"))` to the `daemon` dict in `_build_status_json`.\n\n2. **Elapsed wall-clock seconds, rounded to 1 decimal place**: `_compute_uptime_seconds` uses `(datetime.now() - started_dt).total_seconds()` and `round(elapsed, 1)`.\n\n3. **`null` when `started_at` is absent**: `ds.get(\"started_at\")` returns `None` for missing keys; `if not started_at` returns `None`.\n\n4. **`null` when `started_at` is unparseable**: `datetime.fromisoformat(started_at)` raises `ValueError` on garbage input, caught by `except (ValueError, TypeError)` returning `None`.\n\n5. **uptime_seconds increases between consecutive calls**: Each call invokes `datetime.now()` independently, so elapsed time naturally increases.\n\n6. **Existing daemon fields unchanged**: All pre-existing fields (`pid`, `state`, `cycle`, `current_change`, `current_phase`, `current_project`, `heartbeat`) remain with identical logic.\n\nCode quality: Clean helper function with proper type hints, docstring, and error handling. No dead code, naming is clear and consistent. `json.dumps` correctly serializes `None` as JSON `null`.\n"}
</tool_callTool>