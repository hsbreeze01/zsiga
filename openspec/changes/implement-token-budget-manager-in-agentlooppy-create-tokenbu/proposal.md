# Proposal: implement token budget manager in agent/loop.py: create TokenBudget class that tracks cumulative token usage across turns, enforces per-turn and total-session limits, and triggers compaction proactively when approaching budget threshold rather than after overflow

## Summary
implement token budget manager in agent/loop.py: create TokenBudget class that tracks cumulative token usage across turns, enforces per-turn and total-session limits, and triggers compaction proactively when approaching budget threshold rather than after overflow

## Motivation

## Expected Behavior

