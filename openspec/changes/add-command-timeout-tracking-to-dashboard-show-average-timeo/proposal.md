# Proposal: add command timeout tracking to dashboard: show average timeout rate per phase, highlight phases that timeout most frequently, add a warning indicator when timeout rate exceeds 20%

## Summary
add command timeout tracking to dashboard: show average timeout rate per phase, highlight phases that timeout most frequently, add a warning indicator when timeout rate exceeds 20%

## Motivation

## Expected Behavior

