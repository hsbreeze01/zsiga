# Proposal: create pipeline/dependency.py with ChangeGraph class: constructor takes openspec directory path, method add_change reads targets from proposal.md, method check_conflicts returns list of overlapping file targets between changes, method execution_order returns topologically sorted change names

## Summary
create pipeline/dependency.py with ChangeGraph class: constructor takes openspec directory path, method add_change reads targets from proposal.md, method check_conflicts returns list of overlapping file targets between changes, method execution_order returns topologically sorted change names

## Motivation

## Expected Behavior

